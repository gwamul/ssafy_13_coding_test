시작 시간 : 오전 10시 50분
완료 시간 : 오후 3시
=> 중간에 점심이나 과제를 제외하면 약 2시간

AI 사용 X

포인트 : 
1. Top-down 방식의 dp를 사용


#풀이 과정

이중for문을 돌면서 각 좌표를 recur(i, j)로 dp 계산
재귀 안에서 상하좌우 BFS를 돌면서 만약 dp[][]의 값이 0이 아니라면 이미 최대 경로가 확정된 경우이므로 dp[][]의 값을 반환 => 해당 값에 +1을 한 값 4개(상하좌우)의 max값이 해당 좌표의 dp 값
dp가 0이라면 해당 좌표에서 계속 재귀를 들어가는데 상하좌우 좌표보다 현재 좌표의 대나무 개수가 가장 많은 경우에는 1을 반환


