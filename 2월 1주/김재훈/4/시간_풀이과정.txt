시작 시간 : 오전 9시 10분
완료 시간 : 오전 11시

AI 사용
- DP (Top down, Memoization) 점화식 및 구조

#풀이 과정

포인트 : DP 점화식 세우기 

w와 h의 남은 개수를 각각 w,h로 설정
- w,h < 0이라면 제외
- w == 0이라면 dp[0][h]는 항상 1
- w,h에서의 상황은 (w, h-1) 혹은 (w-1, h+1)이므로
dp[w][h] = dp[w][h-1] + dp[w-1][h+1]




